# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# Local environment files and secrets
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.p12
*.pfx
*.key
*.crt
*.cer
*.mobileprovision
*.jks
*.keystore
*firebase-adminsdk*.json

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/android/build/
/android/*/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
android/key.properties
android/*.jks
android/*.keystore
node_modules

# Local cloud credentials
.credentials/
service-account*.json
*service-account*.json

# Local mobile Firebase config
/config/mobile/*.json
!/config/mobile/*.example.json
!/config/mobile/README.md
/config/mobile/*.plist
/config/mobile/*.env
/config/mobile/*.yaml
# Sensitive Firebase config files (keep local, never commit)
/android/app/google-services.json
/android/app/src/local/google-services.json
/android/app/src/staging/google-services.json
/android/app/src/production/google-services.json
/ios/Firebase/local/GoogleService-Info.plist
/ios/Firebase/staging/GoogleService-Info.plist
/ios/Firebase/production/GoogleService-Info.plist
/web/firebase-messaging-config.js

# Generated smoke-test reports
/docs/smoke-managed-account-auth-*.json
/docs/smoke-public-signup-*.json
/docs/smoke-public-signup.md

# Local release/build artifacts
/artifacts/
/android/.kotlin/

# Local-only folders kept out of Git
/.firebase/
/docs/*
# Exceptions: shared admin/mobile contract docs must be tracked -- the
# cross-repo CI contract check reads these from a fresh clone, and a
# blanket /docs/ ignore silently hid this (and every other doc) from Git.
!/docs/shared-backend-contract.md
!/docs/inter-repo-admin-mobile-runbook.md
# Meme raison : les brouillons de CGU et de politique de confidentialite sont
# du travail produit, relu et repris entre deux sessions. Les laisser hors de
# Git reviendrait a les perdre au premier nettoyage du repertoire de travail.
!/docs/legal-drafts/
# Et meme raison encore pour les runbooks de release : ce sont les seuls
# documents qui disent ce qui a ete verifie avant un build donne, par quelle
# commande et a quelle date. Un dossier de release qui n'existe que sur un
# poste de travail ne prouve rien -- c'est exactement ce qu'on demandera si
# une version en production doit etre expliquee apres coup. Les rapports de
# smoke (docs/smoke-*.json) restent ignores : ils portent des identifiants de
# comptes de test.
!/docs/checklists/
!/docs/play-store-submission-*.md
# Meme raison : scripts/check-android-release-readiness.ps1 lit ce fichier
# depuis un checkout frais pour verifier que les URLs legales (politique de
# confidentialite, suppression de compte) exigees par la Play Console sont
# bien documentees. Aucun secret dedans -- seulement des URLs publiques.
!/docs/store-compliance.md

# Et la specification du socle de recherche, pour une raison de plus : elle
# enregistre des contraintes verifiees dans le code et des mesures faites sur
# la production a une date donnee -- la liste blanche des regles, le fait que
# birthDate soit prive, les deux postes en texte libre a remapper. Un
# document qui n'existe que sur un poste de travail est un document que
# personne ne relira avant de refaire les memes erreurs.
!/docs/talent-search-spec.md

# Et les documents de reprise, pour la raison qui a motive toutes les
# exceptions ci-dessus : ils disent ou une session s'est arretee, ce qui a ete
# verifie contre la production plutot que suppose, et ce qui reste. Un `git
# log` donne les commits, jamais le fait qu'un deploiement a reussi a moitie ni
# qu'un chemin n'a jamais tourne sur un appareil. Laisser cela hors de Git,
# c'est le reperdre a chaque fois.
!/docs/reprise-*.md
